{
  "version": 2,
  "installCommand": "npm ci --no-audit --progress=false",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}


