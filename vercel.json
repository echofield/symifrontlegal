{
  "version": 2,
  "env": {
    "AI_PROVIDER": "openai"
  },
  "installCommand": "npm --prefix symione install --no-audit --progress=false",
  "buildCommand": "npm --prefix symione run build",
  "outputDirectory": "symione/build"
}


